const int LED = 13, POT = A0;

void setup() {
 pinMode(LED, OUTPUT);
}

void loop() {
 int potValue = analogRead(POT);
 int blinkSpeed = (potValue > ???) ? 50 : 1000; 

 digitalWrite(LED, HIGH);
 delay(blinkSpeed);
 digitalWrite(LED, LOW);
 delay(blinkSpeed);
}



